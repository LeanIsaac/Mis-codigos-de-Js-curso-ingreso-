/*
al presionar el botón mostrar 10 repeticiones 
con números ASCENDENTE, desde el 1 al 10.*/

function mostrar()
{

	/*let contador = 0; // primera parte inicializacion


	while(contador < 10) // segunda parte condición
	{
		contador = contador + 1; //3ra parte, modificar la variable de control

		console.log(contador);
	}*/

	/*var num;

		num = 0;

		while (num < 11)
		{
			num = num + 1; 
			console.log(num);
		}

	num = prompt("ingrese un numero");
	num = parseInt(num);

	while (isNaN(num) == true)
	{
		// hay un error
		num = prompt("Error lo que ingresaste no es un numero");
		num = parseInt(num);
	}
	*/
	


	/*
	al presionar el botón mostrar 10 repeticiones 
	con números ASCENDENTE, desde el 1 al 10.*/


	//pida 5 num
	//sumelos y calcule el promedio

	var num;
	var contadorCantidadNumeros;

	contadorCantidadNumeros =0;

	console.log("fuera del while");
	while(contadorCantidadNumeros<5) //condicion
	{
		console.log("dentro del while");
		num =prompt("ingrese un número");
		num = parseInt(num);


		contadorCantidadNumeros = contadorCantidadNumeros +1; //modifico la variable de control
		console.log(contadorCantidadNumeros);
	}












	//while significa "mientras"
}








